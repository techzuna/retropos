{
  "name": "@restro/domain",
  "version": "0.1.0",
  "private": true,
  "//": "Feature code both products share. Anything here must work identically for the self-hosted outlet and the hosted cloud service, so it may not reach for a filesystem, a specific database driver, or anything else only one of them has.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@date-fns/tz": "^1.5.0",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
