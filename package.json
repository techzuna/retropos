{
  "name": "restroreserve",
  "private": true,
  "//": "Monorepo root. Two products share one domain: apps/outlet is the self-hosted box in a restaurant (SQLite), apps/cloud is the hosted multi-tenant service (Postgres). Feature code lives in packages/ so the two cannot drift apart.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "outlet": "npm run --workspace apps/outlet",
    "cloud": "npm run --workspace apps/cloud",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "db:compose": "node packages/db/compose.mjs",
    "db:check": "node packages/db/compose.mjs --check"
  }
}
